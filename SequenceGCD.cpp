#include <bits/stdc++.h>
using namespace std;

int main()
{
    int tlines;
    cin >> tlines;
    while(tlines--)
    {
        int x, y;
        int arr[],
        for (int i = 0; i < x; i++)
        {
            
        } 
    }
    return 0;
}